# ====================================
# Xaytheon Backend — Environment Config
# ====================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# --- Server ---
PORT=5000                          # Port the backend listens on

# --- Authentication ---
JWT_SECRET=change_me_to_a_random_string   # Secret for signing JWTs (use a long random string in production)

# --- Email (for password-reset emails) ---
# You can skip these for local development; password-reset emails just won't send.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_password_or_app_password
EMAIL_FROM='Xaytheon <no-reply@xaytheon.com>'

# --- Frontend URL ---
# The URL where the frontend is served (used for CORS and redirect links)
FRONTEND_URL=http://127.0.0.1:5500

# --- GitHub OAuth (optional) ---
# Required only if you want "Login with GitHub".
# See SETUP_GITHUB_OAUTH.md for step-by-step instructions.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# --- API URL ---
# The public URL of this backend (used for OAuth callbacks)
API_URL=http://127.0.0.1:5000